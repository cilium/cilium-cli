apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  namespace: ${TEST_NAMESPACE}
  name: allow-all
spec:
  endpointSelector: {}
  egress:
  - toEntities:
    - all
  ingress:
  - fromEntities:
    - all
